<% if current_user.present? %>
  <%= button_to "Cerrar sesión", logout_path, method: :delete, data: { turbo: false },
                                              class: "bg-red-500 hover:bg-red-600 text-white font-bold
                                                      py-2 px-4 rounded-lg shadow-md transition duration-300" %>
<% end %>
